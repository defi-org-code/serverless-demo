service: serverless

awsProject: "defi.org by Rotem"

provider:
  name: aws
  runtime: nodejs14.x
  region: us-east-2
  tags:
    Project: ${self:awsProject}
  deploymentBucket:
    tags:
      Project: ${self:awsProject}
  iam:
    role:
      tags:
        Project: ${self:awsProject}
  stackTags:
    Project: ${self:awsProject}

functions:
  demo-function:
    handler: handler.hello
    events:
      - http:
          path: hello
          method: get
          cors: true
