service: serverless

provider:
  name: aws
  runtime: nodejs14.x
  region: us-east-2
  tags:
    Project: "defi.org by Rotem"

functions:
  demo-function:
    handler: handler.hello
    events:
      - http:
          path: hello
          method: get
          cors: true
